import React from 'react';
import { View, Text } from 'react-native';

const Button = () => {
    return (
        <View>
            <Text>Botão</Text>
        </View>
    );
};

export default Button;
